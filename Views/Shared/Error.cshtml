@model ErrorViewModel

@{
    ViewData["Title"] = "Грешка";
    Layout = "_LayoutAdmin";
}

@section breadcrumb {
    <span>@ViewData["Title"]</span>
}

<h1>
    <span class="about-bold">@Model.StatusCode - Грешка на сървъра (<code>@Model.RequestId</code>)</span>
</h1>

<h2>
    <small>@Model.Path</small>
</h2>

<div class="about-small">
    За съжаление се натъкнахте на проблем, чиято причина е изцяло по наша вина.
    <img src="@Url.Content("~/images/internal-server-error.jpg")" alt="@Model.StatusCode - Internal Server Error" class="about-portrait img-responsive">    
    Не би трябвало да виждате тази странца, моля обърнете се към администратора на системата по email за да може да отстрани проблема възможно най-бързо.    
    Моля, при забелязани грешки и неизправности по системата, свържете се с администратора на системата посредством бутона по-долу.    
</div>

<div class="about-button">
    <a class="btn btn-xlarge btn-tales-one" href="mailto:@StranitzaConstants.AdministratorEmail">Свържете се с нас</a>
</div>

<hr>

@if (Model.Error != null && User.Is(StranitzaRoles.Administrator))
{        
    <pre>@Model.Error</pre>
    <pre>@Model.StackTrace</pre>
}